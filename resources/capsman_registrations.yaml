namespace: mikrotik
subsystem: capsman
resource_path: /caps-man/registration-table

global_labels:
  routerboard_address: $HOSTNAME
  routerboard_name: $ALIAS
  interface: $interface

metrics:
  - name: registrations_count
    help: Number of active registration per CAPsMAN interface
    type: GaugeVec
    operation: Inc
    reset_gauge: true
